import React, { useState } from "react";
import { Routes, Route } from "react-router-dom";
import Header from "./Header";

const App = () => {
  return (
    <div>
      <nav>
        <Header />
      </nav>
      <Routes>
        <Route />
      </Routes>
    </div>
  );
};

export default App;
